services:
  app:
    image: ghcr.io/biisal/portfolio/biisal-portfolio:latest
    container_name: nextjs-standalone-pnpm-container
    environment:
      NODE_ENV: production
      PORT: "3001"
    ports:
      - "3001:3001"
    restart: unless-stopped
